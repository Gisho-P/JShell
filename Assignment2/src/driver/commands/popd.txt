POPD(1)                          User Commands                          POPD(1)

NAME
        popd - removes the last directory pushed, and changes the current
        directory to that one

SYNOPSIS
        popd

DESCRIPTION
        Removes the last directory that was pushed to the directory stack,
        and changes the current working directory to this directory