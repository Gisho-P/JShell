PUSHD(1)                         User Commands                         PUSHD(1)

NAME
        pushd - saves the current working directory to the stack

SYNOPSIS
        pushd

DESCRIPTION
        Saves the current working directory to the top of the stack.