Class name: DirStack
Parent class (if any): Object
Subclasses (if any): None

Responsibilities:
* Clear directory stack
* Add path to directory stack
* Pop last stored directory from stack
* Return error message if necessary

Collaborators:
